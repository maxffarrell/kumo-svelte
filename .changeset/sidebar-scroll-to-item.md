---
'kumo-svelte': patch
---

Add `scrollToItem(id, options?)` to `useSidebar()` for imperatively scrolling a tagged nav item into the sidebar viewport. Tag items with the new `itemId` prop on `Sidebar.MenuButton` or `Sidebar.MenuItem`.
